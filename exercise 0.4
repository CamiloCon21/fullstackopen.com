sequenceDiagram

    subgraph Browser
    b((User))
    b -->|write in this textbos and click the button Save| b1(Send new note)
    end

    subgraph Server
    s((Server))
    end

    subgraph Database
    d((Database))
    end

    b1 -->|POST /new_note| s
    s -->|save the no the in the DBA| d
    d -->|Response OK| s
    s -->|Send the response to browser| b1
